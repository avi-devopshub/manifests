apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    env: dev
spec:
  containers:
    - name: nginx-cont
      image: nginx:latest
      ports:
        - name: nginx-port
          containerPort: 80