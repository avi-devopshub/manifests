apiVersion: v1
kind: Pod
metadata:
  name: httpd-pod
  labels:
    env: dev
spec:
  containers:
    - name: httpd-cont
      image: httpd:latest
      ports:
        - name: httpd-port
          containerPort: 80